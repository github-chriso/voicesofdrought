export function ValuePropRibbon() {
  return (
    // Pull the ribbon up 1px to overlap the hero wave and remove any seam
    <section className="relative -mt-px bg-sunshine-orange text-ink py-4">
      <div className="container text-center">
        <p className="font-headline text-lg md:text-xl font-semibold">
          You’ve lived it — now help us plan for the next drought.
        </p>
      </div>
    </section>
  );
}
